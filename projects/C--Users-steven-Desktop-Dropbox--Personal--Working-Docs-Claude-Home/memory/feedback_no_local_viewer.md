# Feedback: No Local Viewer

Never run `python scripts/viewer.py` or open `http://127.0.0.1:8765`.
The user exclusively uses the QB codepage viewer to review forecast and validation results.
Do not launch the local viewer after forecast or validate runs, even when the skill instructions say to do so.
